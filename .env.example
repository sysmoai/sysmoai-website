# SYSmoAI Workspace Root — Global Environment Variables
# Copy to .env and fill in values (never commit .env)

# Database connection string (used by @workspace/db)
# DATABASE_URL=postgresql://user:password@host:5432/sysmoai

# Google Indexing API service account (JSON, optional)
# GOOGLE_INDEXING_SERVICE_ACCOUNT={...}
